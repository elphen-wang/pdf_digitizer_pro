# pdf_digitizer_pro
A GUI tool to extract vector curve data from PDF files. Users can open PDFs, calibrate axes (linear/log scale), and pick curves to capture coordinate points. Features include data filtering, preview, copy, and export to TXT/CSV. Supports English/Chinese UI and offers a magnifier for precise selection. 
